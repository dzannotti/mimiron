#!/bin/bash

# Launch a command in a floating terminal with Mimiron branding
# Usage: mimiron-launch-terminal <command>

cmd="$*"
exec setsid ghostty --window-width=80 --window-height=40 --class=Mimiron --title=Mimiron -e bash -c "mimiron-show-logo; $cmd; mimiron-show-done"
